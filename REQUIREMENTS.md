# 專案需求規格書：免登入多人即時共用記帳系統 (Kahoot 風格)

我想要開發一個多人共用的記帳系統，主要用於朋友出遊或聚餐時一起記帳，並在活動結束後進行最終結算。這個系統的操作體驗需要像 Kahoot 一樣輕量：使用者「不需要註冊/登入」，只需輸入一個「房間代碼 (Room Code)」就能加入同一個群組。

請作為資深全端架構師與資深開發人員，協助我引導、設計並逐步寫出這個系統的程式碼。

---

## 🛠️ 技術堆疊
- 前端：React / Next.js (搭配 Tailwind CSS)
- 後端與資料庫：Supabase (利用 PostgreSQL、Supabase Realtime 進行即時資料同步)

---

## 核心功能與業務邏輯

### 1. 房間與 Session 管理 (免登入機制)
- **加入機制**：使用者進入首頁，可選擇「建立房間」或「輸入房間代碼加入」。
- **身分識別**：由於免登入，當使用者第一次進入房間時，前端需隨機生成一個 UUID，並存入手機瀏覽器的 `localStorage` 中作為該使用者的 Token，同時讓使用者輸入一個暱稱。
- **斷線重連**：若使用者不小心重整網頁或關閉瀏覽器，只要 `localStorage` 的 UUID 還在，回到該房間時就能自動認領原本的身分與暱稱。
- **生命週期**：
  - 房間若連續「閒置一個月」（以最後一筆帳單更新時間或房間活動時間為準），系統將自動關閉/封存。
  - 房間建立者（Host）擁有權限可以手動直接關閉/結算房間。

### 2. 帳單管理與權限控制
- **新增帳單**：房間內的每位成員都可以自由加入新的帳單（Bill）。
- **編輯權限**：只有「新增該帳單的人」事後可以進行編輯。
- **安全刪除/還款機制（雙方確認狀態機）**：帳單不允許直接從資料庫 Hard Delete。必須透過狀態機處理還款流程：
  1. A 幫 B 墊款 (狀態：`Unpaid`)
  2. B 實際還錢給 A 後，B 在系統按下「我已還款」(狀態：`Pending_Confirm`)
  3. A 收到通知，在系統按下「確認收款」(狀態：`Settled`)。只有到達 `Settled` 狀態，該筆交易才算完成。

### 3. 多幣別與匯率計算
- **基準貨幣**：房間建立時需設定一個基準貨幣（例如 TWD）。
- **多幣別輸入**：成員新增帳單時可選擇其他幣別（例如 JPY、USD）。
- **匯率處理**：系統需串接外部免費匯率 API (如 ExchangeRate-API)，在新增帳單時抓取當下匯率。資料庫需同時記錄「原始幣別/金額」以及「換算後的基準貨幣金額」，避免日後匯率浮動影響結算。

### 4. 分帳最佳化演算法 (Debt Simplification)
- 系統需內建分帳最佳化邏輯（使用圖論中的貪婪演算法 Greedy Algorithm）。
- 舉例：若 A 欠 B 100 元，B 欠 C 100 元，系統最終結算時應簡化為「A 直接給 C 100 元」，以最小化還款總次數。

### 5. 即時同步與操作紀錄 (Action Log)
- 利用 Supabase Realtime 功能，當有人新增、修改帳單或變更還款狀態時，房間內所有成員的前端畫面需即時更新。
- 房間內需有一個簡單的「動態牆」，即時顯示操作紀錄（例如：「小明 新增了一筆 晚餐 500 元」、「小華 已確認 小明的還款」）。

---

## 🚀 協作開發計畫

為了確保開發品質，請不要一次給出所有的程式碼。我們將分階段進行。現在，請先執行**【階段一】**：

1. 針對上述需求，設計出最適合 Supabase (PostgreSQL) 的**資料庫 Schema (欄位與資料型態)**，包含 `rooms`, `users`, `bills`, `debts`, `action_logs` 等資料表。
2. 請特別考慮到 Supabase 的 RLS (Row Level Security) 策略，如何利用我們存在前端的 UUID 來確保「只有建立者能編輯帳單」與「只有 Host 能關閉房間」的安全機制。

請輸出階段一的資料庫設計與 SQL 語法，並說明你的設計思考。